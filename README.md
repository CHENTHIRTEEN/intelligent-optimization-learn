# intelligent-optimization-learn
存放智能优化算法学习代码

## GitHub Push 规则

本项目使用 `uv` 管理依赖，提交代码时遵循以下规则：

- 只提交：源代码、`README.md`、`pyproject.toml` 等项目配置文件。
- 不提交：`uv` 生成文件（如 `uv.lock`、`.python-version`）以及虚拟环境目录（`.venv/`、`venv/`）。
- 推送前建议执行：`git status`，确认没有临时文件和环境文件进入暂存区。

对应忽略规则已写入 `.gitignore`。
